'use client';

// Notifications-service is a core platform service (like auth-api and subscriptions-api).
// It does NOT require a subscription — all tenants have unrestricted access.
// WhatsApp API connectivity is an integration-level concern handled separately.
export function SubscriptionBanner() {
  return null;
}
